#include <stdio.h>
#include "main.h"

/**
*_putchar - write char c to stout
*@c: the char to print
*Return: on success 1.
*On error: -1 is returned and error is set appropriatly.
*/
int _putchar(char c)
{
return (write(1, &c, 1));
}
